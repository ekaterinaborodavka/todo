import styled from "styled-components/macro";

const StyledTitle = styled.span`
  font-family: Segoe UI;
  color: white;
  margin-left: 4rem;
  font-weight: 600;
  line-height: 3rem;
`;

export { StyledTitle };
